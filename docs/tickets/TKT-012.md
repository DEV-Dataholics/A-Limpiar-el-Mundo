# Ticket: TKT-012 - Homologación de Header Institucional, Fondo Orgánico con Cactus en Registro y Corrección de Selectores de Ubicación

**Autor / Rama**: `feature/2026-09-08-landing-a-limpiar-el-mundo`  
**Módulos Afectados**: Frontend UI / Registro (`RegisterView`), Inicio de Sesión (`LoginView`), Landing (`LandingView`), Componente Reutilizable (`Navbar`), Selector de Ubicación (`LocationSelector`), Dashboards (`AdminDashboardView`, `DashboardView`, `MetricsDashboardView`)  
**Tipo de Cambio**: Feature UI | Fix | Refactor  

---

### 1. ALERTA DE IMPACTO EN BASE DE DATOS (CRÍTICO)
- [x] **¿Modifica esquemas existentes?**: NO
- [x] **¿Altera Llaves Primarias (PKs) o Auto-incrementables?**: NO
- [x] **¿Modifica Llaves Foráneas (FKs) o restricciones de integridad?**: NO
- [x] **¿Requiere nueva migración en backend?**: NO
- [x] **Detalle de cambios DDL**: N/A (Cambios estrictamente a nivel de presentación en Frontend).

---

### 2. IMPACTO EN BACKEND (API)
- **Endpoints**: Mantiene contrato íntegro con `POST /api/auth/register` y `POST /api/auth/login`.
- **Configuración de Seguridad (`api/.env`)**: Se configuró la clave requerida `JWT_SECRET` para permitir la emisión y verificación de tokens JWT en el inicio de sesión.
- **Semillas de Acceso (`AdminSeeder.php`)**: Actualizadas credenciales institucionales de prueba para perfiles Administrador y Voluntario (`admin@alimpiarelmundo.org.mx` y `voluntario@alimpiarelmundo.org.mx`).
- **Políticas Shield / Roles RBAC**: Preservados `role_id: 1` (Admin) y `role_id: 2` (Voluntario).
- **Compatibilidad con Contratos de API**: Totalmente preservada.

---

### 3. IMPACTO EN FRONTEND
- **Componentes y Vistas Afectadas**:
  - `frontend/src/components/Navbar.tsx`: Nuevo componente institucional compartido con logos oficiales (United Way Chihuahua 35 Aniversario + A Limpiar el Mundo 2026) y enlaces de navegación, garantizando consistencia idéntica y sin variación entre vistas.
  - `frontend/src/views/LandingView.tsx`: Integra el componente `Navbar` compartido.
  - `frontend/src/views/RegisterView.tsx`: Integra `Navbar`, incorpora `BotanicalCactus` y `BotanicalSprout` con parallax reactivo al mouse detrás de la tarjeta, elimina etiqueta duplicada y actualiza textos institucionales.
  - `frontend/src/views/LoginView.tsx`: Integra el componente `Navbar` compartido y pie de página institucional.
  - `frontend/src/components/LocationSelector.tsx`: Corrige fondo negro hardcodeado (`bg-[#001224]`) migrando a `bg-white` con bordes `#D8E2F0`, ring de enfoque `#0044B5`, textos legibles y soporte para prop `showLabels`.
  - `frontend/src/views/AdminDashboardView.tsx`, `DashboardView.tsx`, `MetricsDashboardView.tsx`, `ImpactFeedGrid.tsx`: Reemplazo de todas las menciones y logos remanentes de "Somos Comunidad" por la identidad oficial "A Limpiar el Mundo 2026 / United Way Chihuahua".
- **Consumo de API centralizada**: Vía cliente existente y `API_URL`.
- **Manejo de Estado / Caché**: Sin cambios de estado global.

---

### 4. CHECKLIST PREVIO A COMMIT
- [x] Código verificado en local con Laragon y Vite dev server.
- [x] Cero llamadas residuales o URLs duras a servidores externos o IPs locales.
- [x] Sin romper tipado TypeScript (`npx tsc --noEmit` exitoso sin errores).
- [x] Homologación de Header validada en todas las vistas.
- [x] Resumen claro redactado.
