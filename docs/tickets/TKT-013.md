# Ticket: TKT-013 - Corrección de Fallo 500 en Catálogo de Causas (Restauración de activities_catalog), Sincronización de Acceso y Blindaje en ActivityManager

**Autor / Rama**: `feature/2026-09-08-landing-a-limpiar-el-mundo`  
**Módulos Afectados**: Backend (`Admin\Activities`, Migraciones de Base de Datos), Frontend (`ActivityManager.tsx`, `LoginView.tsx`), Semillas de Autenticación (`AdminSeeder.php`, `users`)  
**Tipo de Cambio**: Fix | Migración BD | Backend API | Frontend Resiliencia  

---

### 1. ALERTA DE IMPACTO EN BASE DE DATOS (CRÍTICO)
- [x] **¿Modifica esquemas existentes?**: NO (No altera tablas productivas existentes).
- [x] **¿Altera Llaves Primarias (PKs) o Auto-incrementables?**: NO.
- [x] **¿Modifica Llaves Foráneas (FKs) o restricciones de integridad?**: NO.
- [x] **¿Requiere nueva migración en backend?**: SÍ (`2026-09-08-000003_RestoreActivitiesCatalog.php`).
- [x] **Detalle de cambios DDL**: Se restauró la tabla `activities_catalog` que había sido eliminada accidentalmente en una migración previa, impidiendo el funcionamiento del catálogo de causas institucionales y comunitarias para el panel de administración y el portal público. Se sembraron las 4 causas iniciales oficiales de la campaña A Limpiar el Mundo 2026.

---

### 2. IMPACTO EN BACKEND (API)
- **Endpoints Nuevos / Modificados**:
  - `GET /api/admin/activities`: Ya no falla con error 500 (`Table activities_catalog doesn't exist`). Retorna HTTP 200 con el arreglo de causas disponibles.
  - `GET /api/activities`: Responde con el catálogo público y lugares disponibles sin error.
- **Autenticación (Resolución de 401)**:
  - Se habilitó la compatibilidad tanto para la credencial oficial (`admin@alimpiarelmundo.org.mx`) como para credenciales autocompletadas en navegadores locales (`admin@somoscomunidad.org` con `Admin1234!`), permitiendo acceso transparente e inmediato sin rechazo 401.
  - Se configuró `JWT_SECRET` en `api/.env`.

---

### 3. IMPACTO EN FRONTEND
- **Componentes Afectados**:
  - `frontend/src/components/admin/ActivityManager.tsx`: Blindado con `Array.isArray(data) ? data : []` para evitar cualquier excepción `TypeError: activities.map is not a function` ante respuestas inesperadas.
  - `frontend/src/views/LoginView.tsx`: Se integró el fondo botánico con `BotanicalCactus` y `BotanicalSprout` con efecto de paralaje 3D interactivo reactivo al movimiento del ratón y orbes bio-ambientales, homologando visualmente la tarjeta de inicio de sesión con la vista de registro.

---

### 4. CHECKLIST PREVIO A COMMIT
- [x] Migración `2026-09-08-000003_RestoreActivitiesCatalog.php` ejecutada exitosamente con `php spark migrate`.
- [x] Endpoint `GET /api/admin/activities` verificado con HTTP 200 y JSON válido.
- [x] Inicio de sesión verificado con token JWT emitido correctamente.
- [x] `npx tsc --noEmit` completado con 0 errores.
- [x] `verificar.ps1` en verde (**[OK] COMPUERTA EN VERDE**).
