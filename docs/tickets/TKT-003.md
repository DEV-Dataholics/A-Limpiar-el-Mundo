# Ticket: [TKT-003] Rediseño de Hero con Parallax 3D, Ingesta Fotográfica y Planta Vectorizada

**Autor / Rama**: `feature/2026-09-08-landing-a-limpiar-el-mundo`  
**Módulos Afectados**: Frontend (`LandingView.tsx`, `BotanicalPlant.tsx`, `public/campana-*.jpg`)  
**Tipo de Cambio**: Feature UI / Interacción Visual Avanzada  

---

### 1. ALERTA DE IMPACTO EN BASE DE DATOS (CRÍTICO)
- [x] **¿Modifica esquemas existentes?**: NO
- [x] **¿Altera Llaves Primarias (PKs) o Auto-incrementables?**: NO
- [x] **¿Modifica Llaves Foráneas (FKs) o restricciones de integridad?**: NO
- [x] **¿Requiere nueva migración en backend?**: NO
- [x] **Detalle de cambios DDL**: Ninguno.

### 2. IMPACTO EN BACKEND (API)
- **Endpoints Nuevos / Modificados**: Ninguno.
- **Políticas de Autenticación / RBAC**: Sin cambios.
- **Compatibilidad con Contratos de API**: Totalmente compatible.

### 3. IMPACTO EN FRONTEND
- **Componentes y Vistas Afectadas**:
  - `BotanicalPlant.tsx`: Nuevo componente con ilustraciones vectoriales SVG botánicas estilizadas (`BotanicalPlant` y `BotanicalSprout`) con follaje tipo Monstera en verdes institucionales (`#009464`, `#82D8A4`, `#C6F7DA`), montadas detrás de la foto en plano `-z` y vinculadas a la física de paralaje orgánico (`translate3d`, `rotate`, `swayOffset`).
  - `LandingView.tsx`:
    - Sustitución del esquema circular estático anterior por un split-screen 3D con perspectiva CSS (`perspective: 1200`).
    - Parallax dinámico reactivo al cursor: cálculo de `mousePos` normalizado `[-1, 1]`, `tiltX`, `tiltY`, `shiftX` y capas de profundidad diferenciada (badges flotantes, tarjeta fotográfica y fondo botánico).
    - Ingesta de fotografías reales de campaña desde `docs/` (`A Limpiar el Mundo 2025-07.jpg`, `04`, `08`) a `frontend/public/` para visualización en alta definición.
- **Consumo de API centralizada**: Sin cambios.
- **Manejo de Estado / Caché**: Estado local en React para `mousePos`, `isHovered` y `activePhoto`.

### 4. CHECKLIST PREVIO A COMMIT
- [x] Código verificado en local con servidores activos.
- [x] Cero llamadas residuales o URLs duras a servidores externos.
- [x] Sin romper tipado TypeScript (`npx tsc --noEmit`).
- [x] Sin errores de linter (`npx oxlint`).
- [x] Compuerta de calidad ejecutada (`.\verificar.ps1` en VERDE).
- [x] Resumen claro redactado.
